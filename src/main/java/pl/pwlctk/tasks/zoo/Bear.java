package pl.pwlctk.tasks.zoo;

public class Bear implements Animal{

    private boolean isAlive;

    @Override
    public boolean isAlive(Animal animal) {
        return isAlive;
    }

    @Override
    public double weightning(Animal animal) {
        return 0;
    }
}
